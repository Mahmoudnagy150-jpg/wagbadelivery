# Sea Gull OWNER PRO V22.1

- Branch selection is now a normal checkout field, alongside name/phone/address.
- No branch selector appears in the cart.
- No separate “choose branch first” blocking screen.
- Branch uses a friendly placeholder and inline field validation.
- Selected branch is read directly from the checkout field when submitting.
- Branch selection is cleared after a successful order so the next customer starts clean.
