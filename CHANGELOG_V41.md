# Sea Gull V41 — إصلاحات هذه الجولة + خارطة طريق للمطلوب

## تم إنجازه فعليًا في هذه النسخة (أكواد حقيقية شغالة، مش وعود):

### 1) الطلبات مش بتسمع لحظيًا إلا بعد Refresh — تم حل السبب الجذري
السبب الحقيقي: كل المتصفحات (Chrome/Firefox/Safari) بتعمل "Throttling" لمؤقتات الـ `setInterval` في أي تبويب مش في المقدمة (Background Tab) — يعني لو شاشة الفرع شغالة بس التبويب مش هو الظاهر حاليًا (أو الشاشة مقفولة/التطبيق مصغّر)، المتصفح ممكن يوقف تحديث الطلبات والتنبيهات تلقائيًا لحد ما ترجع تفتح التبويب، وده بالظبط اللي كان بيحصل معاك.
**الحل:** تم نقل عملية "مراقبة الطلبات الجديدة" بالكامل إلى **Web Worker** (خيط تشغيل منفصل لا يتأثر بسياسة الـ Background Throttling)، فبقى الفحص شغال باستمرار كل ثانيتين بغض النظر عن كون الشاشة مفتوحة قدامك أو لأ، والصوت والتنبيه بيشتغلوا فورًا لحظة وصول الطلب. في نظام احتياطي (Fallback) للمتصفحات القديمة جدًا لو مش بتدعم Web Workers.

### 2) الشاشة البيضاء اللي بتظهر على جنب عند فتح لوحة تحكم الفروع — تم حلها
السبب: كان في كود بيفتح نافذة متصفح فارغة (`about:blank`) بشكل مقصود عند تسجيل الدخول، وتفضل شغالة وفاضية على جنب الشاشة (زي "شاشة بيضاء") لحد ما حد يدوس زرار طباعة يدوي. الطباعة التلقائية أصلًا مش بتحتاج النافذة دي إطلاقًا (بتستخدم إطار مخفي تمامًا). تم حذف فتح النافذة الفارغة دي نهائيًا — الطباعة اليدوية دلوقتي بتفتح نافذتها وقت الضغط على الزرار مباشرة بدون أي شاشة بيضاء دايمة.

### 3) الطباعة التلقائية — تقوية الموثوقية (مع توضيح مهم)
نفس مشكلة الـ Throttling في البند 1 كانت بتأثر كمان على تشغيل الطباعة التلقائية (لأنها كانت بتعتمد على نفس المؤقت المتأثر بالتقييد). دلوقتي بقت الطباعة التلقائية بتتفعّل من نفس نظام الـ Worker المستقر، يعني هتشتغل حتى لو الشاشة مش في المقدمة.
**ملاحظة تقنية مهمة يجب معرفتها:** أي متصفح إنترنت (كروم، سفاري، إلخ) **لازم يفتح نافذة تأكيد الطباعة** (Print Dialog) — دي سياسة أمان عالمية في كل المتصفحات ومفيش موقع في الدنيا يقدر يطبع "بصمت" بدون أي نافذة تأكيد من غير برنامج خاص مُثبّت على جهاز الفرع (زي QZ Tray) أو تشغيل الكمبيوتر في وضع Kiosk خاص بالطباعة. لو عايزين طباعة صامتة 100% بدون أي ضغطة تأكيد، ده محتاج تجهيز إضافي على جهاز/متصفح الفرع (تقدر تقولي وهوضحلك الخطوات).

### 4) رقم الأوردر بدل "سيريال اليوم"
بالفحص لقيت إن ده تم فعلاً من نسخة سابقة (V39) — في كل الشاشات ظاهر "أوردر رقم #كذا" مش "سيريال". لو لسه شايف كلمة "سيريال" في أي مكان محدد قولي بالظبط فين وهصلحها فورًا.

### 5) بعد تأكيد السعر النهائي، العميل يروح على طول لمتابعة الطلب من غير ما تفتح له قائمة الطلب/المنيو تاني
كانت الصفحة بتفتح من الأول (المنيو + السلة) وتتبع الطلب موجود تحتها في الصفحة، فالعميل لازم "يسكرول" لتحت عشان يشوفه. تم عمل تحويل تلقائي فوري: أي رابط فيه `?track=` هيخفي المنيو/السلة بالكامل ويورّي شاشة تتبع الطلب مباشرة وبس.

### 6) نغمة تنبيه الطلب الجديد — ثابتة وغير قابلة للتغيير
تأكدت إنها بالفعل مقفولة زي ما طلبت من نسخة سابقة (لا يوجد أي زرار رفع نغمة، وأي محاولة تعديل عبر الـ API بترجع "نغمة الإنذار ثابتة ولا يمكن تغييرها"). لا حاجة لتعديل.

### 7) بحث في صفحة "العملاء" بالاسم / رقم الهاتف / اليوم
تمت الإضافة — خانة بحث بالاسم أو الهاتف + فلتر تاريخ آخر طلب، فوق جدول العملاء مباشرة.

---

## المطلوب اللي لسه محتاج شغل — وده حجمه كبير جدًا ومحتاج مرحلة مخصوصة له

اللي طلبته في البنود دي مش "تعديلات صغيرة" — كل بند منها فعليًا مشروع فرعي متكامل بيحتاج تصميم قاعدة بيانات جديد (جداول جديدة) + شاشات جديدة كاملة في الفرونت إند + منطق حسابي جديد في الباك إند. عشان أضمن إني أسلّمهملك شغالين 100% وبمستوى احترافي حقيقي (مش نصهم شغال ونصهم فيه مشاكل)، أفضل أعمل كل واحد منهم في جلسة مخصوصة له بالترتيب ده:

1. **طرق التصنيع/التسوية بسعر إضافي لكل صنف** (بند 5) — يحتاج: جدول "طرق تصنيع" مرتبط بكل صنف، سعر إضافي لكل طريقة، إضافتها لواجهة العميل عند اختيار الصنف، وشاشة للأونر/الفرع لإضافة/تعديل طرق التصنيع وأسعارها بحرية.
2. **عرض الوزن/العدد/الريسبي/المكونات لكل صنف للعميل** (بند 8) — يحتاج حقول جديدة لكل صنف + تصميم لعرضها بشكل جذاب في صفحة الصنف.
3. **مخزون وجرد فعلي مرتبط بالريسبي والمكونات** (بند 9) — الأكبر والأهم: يحتاج جدول "مكونات خام" (Ingredients) بكمياتها، وربط كل صنف/طريقة تصنيع بالمكونات اللي بتستهلكها بكمية معينة، بحيث كل عملية بيع تخصم تلقائيًا من رصيد المكونات الخام، مع تنبيهات نواقص وتقارير جرد حقيقية.
4. **إعادة تصميم شاشة تعديل المنيو بشكل أبسط (قائمة أقسام قابلة للطي)** (بند 6) — مرتبطة مباشرة ببنود 1 و2 و3 لأن شاشة تعديل الصنف هتكبر وتشمل كل التفاصيل الجديدة دي.
5. **تبسيط/تقوية شاشة لوحة تحكم الفرع** (بند 3) — الأفضل يتعمل بعد ما نعرف بالظبط الأدوات الجديدة (من البنود اللي فوق) اللي هتتضاف، عشان أصمم الشاشة نضيفة من الأول بدل ما أعدلها مرتين.
6. **تقوية لوحة الأونر الكاملة (مخزون/أصناف/أسعار)** (بند 11 الثاني) — نفس الكلام، بيُبنى فوق البنود التقنية اللي فوق.

اقتراحي: نبدأ بالبند رقم 1 (طرق التصنيع + الأسعار) والبند 3 (المخزون الحقيقي) لأنهم أساس كل حاجة تانية، وبعدين نبني عليهم شاشات المنيو ولوحة التحكم. قولّي تحب نبدأ بإيه الأول وهكمل خطوة خطوة باحترافية كاملة بدل ما أعمل حاجة سريعة وناقصة.
