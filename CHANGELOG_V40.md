# Sea Gull V40 — إصلاح التضارب بين واجهة العميل / واجهة المالك / لوحة تحكم الفروع

فحص شامل لكل ملفات النظام (index.php, admin.php, api.php) كشف عن الأسباب الحقيقية التالية لمشاكل التزامن والتحكم بين الواجهات، وتم إصلاحها جميعًا:

## 1) أخطر مشكلة: كلمة مرور دخول كل فرع كانت بترجع تلقائيًا لكلمة السر الافتراضية
كان الكود في `api.php` بينفّذ استعلام `INSERT ... ON CONFLICT DO UPDATE` على جدول الفروع **مع كل طلب/كل تحميل صفحة**، وده كان بيعيد كتابة اسم وكلمة مرور كل فرع على القيم الافتراضية القديمة تلقائيًا. النتيجة: أي محاولة لتغيير باسورد فرع كانت بترجع لوحدها بعد أول Request جديد. تم تغييرها إلى `INSERT OR IGNORE` بحيث تُزرع القيم الافتراضية مرة واحدة فقط عند إنشاء قاعدة البيانات، وبعدها تبقى أي تعديلات فعلية.

## 2) لا توجد أي وسيلة لتغيير كلمة مرور فرع أو حساب المالك
رغم أن قاعدة البيانات والصلاحيات كانت جاهزة، لم يكن هناك أي API أو زر في الواجهة لتغيير كلمة مرور فرع معيّن، ولا لتغيير اسم/كلمة مرور المالك (كانت مكتوبة داخل الكود نفسه بدون أي شاشة لتعديلها). تمت إضافة:
- إجراء `set_branch_password` (Owner فقط).
- إجراء `set_owner_credentials` (يتطلب كلمة المرور الحالية للتأكيد).
- شاشة فعلية لكل ده داخل لوحة المالك.

## 3) حقل "كلمة مرور لوحة التحكم" في الإعدادات كان وهميًا تمامًا
كان فيه حقل في صفحة الإعدادات اسمه "كلمة مرور لوحة التحكم" يمكن تعديله وحفظه، لكنه **غير مرتبط بأي منطق دخول فعلي** — تغييره لا يفعل شيئًا إطلاقًا، وهذا كان مصدر لبس كبير (المستخدم يغيّر كلمة السر معتقدًا أنها أثّرت، بينما تسجيل الدخول الفعلي يعتمد على جدول حسابات الموظفين/الفروع/المالك). تم حذف الحقل نهائيًا واستبداله بنظام تغيير بيانات دخول حقيقي وفعّال (البند 2).

## 4) لوحة "الإدارة المركزية" الخاصة بالمالك كانت بلا أي أدوات فعلية لإدارة الفروع والموظفين
كل الـ Backend الخاص بإدارة الفروع (فتح/غلق فرع، تعديل الموقع والمواعيد، تخصيص منيو/أسعار كل فرع، إدارة حسابات الموظفين) كان **موجودًا وجاهزًا في api.php لكن بدون أي واجهة تستخدمه إطلاقًا**. تمت إضافة قسمين جديدين في صفحة "👑 الإدارة المركزية":
- **إدارة الفروع**: فتح/إغلاق كل فرع، تعديل الموقع (Lat/Lng) والمواعيد، تغيير كلمة مرور دخول الفرع، وتخصيص توافر/سعر كل صنف لكل فرع مع زر تصفير للسعر الأساسي.
- **حسابات موظفي الفروع**: إضافة/تعديل/إيقاف حسابات (مدير فرع، كاشير، مطبخ، دليفري) لكل فرع.

## 5) حالة الفرع (مفتوح/مغلق) في جدول أداء الفروع كانت دائمًا تظهر "مفتوح" حتى لو كان الفرع مُغلقًا فعليًا
كان استعلام `owner_dashboard` لا يجلب عمود `active` من جدول الفروع، فكانت الواجهة تفترض دائمًا القيمة الافتراضية (مفتوح). تم إصلاح الاستعلام ليعرض الحالة الحقيقية.

## ملاحظة
لم يتم المساس بأي بيانات موجودة في `orders.sqlite` (الطلبات، العملاء، التقييمات، المنيو) — كل التعديلات في منطق الكود فقط.
