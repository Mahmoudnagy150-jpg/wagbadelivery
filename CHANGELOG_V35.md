# Sea Gull V35 — Branch Gate Fix

- Removed the duplicate selected-branch badge from the customer hero.
- Customer pages now start with no remembered branch, so branch-specific menu/prices cannot appear before an explicit branch selection.
- Branch picker opens automatically on a fresh customer visit and selecting a branch closes it and loads that branch menu immediately.
- Added default GPS coordinates for all seven branches, only when coordinates are missing; Owner-entered coordinates are preserved.
- Kept the existing project structure, database, customer security, staff roles, workflow, audit trail, KDS, and WhatsApp features intact.
